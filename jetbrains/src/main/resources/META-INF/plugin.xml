<idea-plugin>
  <id>com.github.andrebrait.invariant</id>
  <name>Invariant</name>
  <vendor url="https://github.com/andrebrait">André Brait</vendor>

  <description><![CDATA[
    <p>Invariant is a semantic dark editor color scheme based on Monokai.</p>
    <p>Color identifies what a symbol is; formatting shows how it is being used. Common code stays relatively quiet, while types, parameters, methods, reserved names, annotations, and comments remain easy to find.</p>
    <p>The scheme provides explicit coverage for Java, Kotlin, Python, CSS, and shell scripts, with Language Defaults for other supported languages.</p>
  ]]></description>

  <change-notes><![CDATA[
    <p>Adds explicit markup-language colors across the editor ports.</p>
    <ul>
      <li>Markdown headings are green and bold italic; link text is cyan, destinations are parameter blue, and reference labels are pink.</li>
      <li>HTML and XML tag names read as keywords, attribute names are green, custom tags are orange, and entity references are pale blue.</li>
      <li>YAML anchors and aliases are pale blue, and shell here-document delimiters are bold orange.</li>
    </ul>
  ]]></change-notes>

  <depends>com.intellij.modules.platform</depends>

  <extensions defaultExtensionNs="com.intellij">
    <bundledColorScheme path="colorSchemes/invariant" id="Invariant"/>
  </extensions>
</idea-plugin>
